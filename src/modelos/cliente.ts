export class Cliente {
    nombre: string
    email: string
    telefono: number
    organizacion: string
    constructor(nombre: string, email: string) {
        this.nombre = nombre
        this.email = email
    }
}